apiVersion: route.openshift.io/v1
kind: Route
metadata: { name: tenant-billing-api }
spec:
  to: { kind: Service, name: tenant-billing-api }
  port: { targetPort: 8080 }
  tls: { termination: edge, insecureEdgeTerminationPolicy: Redirect }
